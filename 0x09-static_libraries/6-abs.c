#include "main.h"

/**
 * _abs - function computes absolute val of interger
 * @n: interger to compute value
 * Return: n absolute value
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
